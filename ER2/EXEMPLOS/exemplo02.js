/* Existem dois tippos de laços: o laços contados, e os laços com
variáveis de controle
*/

// while - enquanto (pode ser usado com contadores ou variáveis de controle)

let contador = 0; //contador iniciando em 0

while (contador < 100) // precisa de uma condição
{
    console.log(contador)
    contador = contador + 1; //incremento
}
